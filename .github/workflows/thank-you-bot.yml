name: Legacy issue automation (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This legacy workflow is disabled. Use .github/workflows/auto-comment.yml."
