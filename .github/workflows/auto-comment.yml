name: Disabled duplicate workflow

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Duplicate issue automation disabled to prevent repeated comments."
