name: Auto Comment and Close Issues (disabled legacy)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in favor of auto-comment-and-close.yml"
