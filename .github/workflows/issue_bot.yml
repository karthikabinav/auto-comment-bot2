name: Issue Automation (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This legacy workflow is disabled. Use .github/workflows/auto-comment.yml."
