name: Auto comment and close feedback/suggestion issues (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled in favor of auto-comment-and-close.yml"
